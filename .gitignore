# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets
.env
.env.*
secrets/

# Data (grandes / generados) — versionar solo el generador, no los outputs
data/generated/
data/raw/
*.parquet

# Node / frontend
node_modules/
.next/
dist/

# Observability / local
*.log
.langfuse/

# Eval runs agénticos (D) — artefactos generados por corrida (sin PII); no versionar en git.
backend/tests/evals/runs/
backend/.deepeval/
.deepeval/

# Agent skills instalados por npx (tooling local, no producto — re-instalar con
# `npx skills add vercel-labs/agent-skills`). No vendorizar en el repo.
.agents/
skills-lock.json
.claude/skills/vercel-*
.claude/skills/web-design-guidelines
.claude/skills/writing-guidelines
.claude/skills/deploy-to-vercel

# OS / IDE
.DS_Store
.idea/
.vscode/
