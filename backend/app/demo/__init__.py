"""Demo scaffolding — seeder de datos para la vitrina (NO producción).

Vive fuera de `dashboard/` a propósito: el seeder arma casos con los
componentes reales (rules/fraude), y `dashboard/` NO debe importar `rules/`
(invariante estructural P1/P2). Este módulo es el único autorizado a hacerlo
para poblar datos demo.
"""
